# database connection
database = dict(
    username = 'db_username',
    password = 'db_password',
    host = '127.0.0.1',
    port = '5432',
    db = 'db_name'
)
# application settings
app = dict(
    envirement = 'test',
    engine = dict(
        debug = True,
        displau = False,
    )
)